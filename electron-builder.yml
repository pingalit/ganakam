appId: com.pingalit.ganakam
productName: Ganakam
copyright: Copyright © 2026 pingalit

directories:
  buildResources: build
  output: dist

files:
  - out/**/*
  - "!**/*.map"

win:
  target:
    - target: nsis
      arch: [x64]
  artifactName: "${productName}-${version}-setup.${ext}"

mac:
  target:
    - target: dmg   # initial install
      arch: [x64, arm64]
    - target: zip   # required for electron-updater OTA on macOS
      arch: [x64, arm64]
  artifactName: "${productName}-${version}.${ext}"
  category: public.app-category.finance

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true

dmg:
  window:
    width: 540
    height: 380

# Publish to GitHub Releases (used by electron-updater)
publish:
  provider: github
  owner: pingalit
  repo: ganakam
