name: Build (Test)

# Run automatically on push to main, OR trigger manually from the Actions tab
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      platform:
        description: Platform to build
        required: true
        default: both
        type: choice
        options: [both, windows, mac]

permissions:
  contents: read

jobs:
  build-windows:
    if: >
      github.event_name == 'push' ||
      inputs.platform == 'both' ||
      inputs.platform == 'windows'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build && npx electron-builder --win --publish never
      - uses: actions/upload-artifact@v4
        with:
          name: ganakam-windows-${{ github.sha }}
          path: dist/*.exe
          retention-days: 14

  build-mac:
    if: >
      github.event_name == 'push' ||
      inputs.platform == 'both' ||
      inputs.platform == 'mac'
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      # macos-latest is arm64 (Apple Silicon). Build arm64-only to avoid
      # hdiutil failures when cross-compiling x64 DMG on an arm64 runner.
      - run: npm run build && npx electron-builder --mac --arm64 --publish never
      - uses: actions/upload-artifact@v4
        with:
          name: ganakam-mac-${{ github.sha }}
          path: dist/*.dmg
          retention-days: 14
